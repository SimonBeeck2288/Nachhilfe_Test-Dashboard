# Survey Task 3: Test Suite & Infrastructure Specification Mining

## Target Scope
Investigate project test infrastructure and existing tests (`src/tests/`):
- Read `package.json` to identify test runner scripts (`npm run test`, `npm run lint`), vitest configuration, and dependencies.
- Map all existing test files in `src/tests/` and summarize existing test coverage across components, questions, state, and evaluation logic.
- Identify test utilities, mocks, and setup files.
- Determine required new test cases for Pause pool countdown/exhaustion, question bookmarking, back button navigation history, decimal numeric comparison, and question bank fixes.

## Handoff Requirements
Write your detailed specification report to `c:/Users/beeck/git/repos/NachhilfeTest/.agents/spec_miner_survey_3/analysis.md` and complete `handoff.md`.
